---
title: CLI
description: Trình khách command-line cho nền tảng S.EE
icon: Terminal
---

Một trình khách command-line cho nền tảng S.EE, cho phép bạn tạo và quản lý URL ngắn, đoạn văn bản và tệp trực tiếp từ terminal.

- [sdotee/cli](https://github.com/sdotee/cli)

## Tính năng

- **Short URL Management**: Tạo, cập nhật và xóa liên kết rút gọn với slug tùy chỉnh, tên miền và thời gian hết hạn
- **Text Snippets**: Chia sẻ đoạn mã và ghi chú từ stdin hoặc file
- **File Upload**: Tải tệp lên trực tiếp hoặc qua stdin
- **Domain & Tag Management**: Liệt kê tên miền khả dụng và quản lý tag
- **Password Protection**: Bảo vệ nội dung bằng mật khẩu
- **JSON Output**: Đầu ra có thể đọc bởi máy cho scripting

## Cài đặt

### macOS

```bash
brew tap sdotee/homebrew-tap
brew install see
```

### Debian / Ubuntu

```bash
dpkg -i see-cli_<version>_<arch>.deb
```

### RHEL / Fedora

```bash
rpm -i see-cli_<version>_linux_<arch>.rpm
```

### Arch Linux

```bash
yay -S see-cli
# or pre-built binary
yay -S see-cli-bin
```

### Từ source

```bash
go install github.com/sdotee/cli@latest
```

Tải các binary và package dựng sẵn từ [GitHub Releases](https://github.com/sdotee/cli/releases).

## Cấu hình

Đặt API key của bạn qua environment variable hoặc flag:

```bash
# Environment variable (recommended)
export SEE_API_KEY="your-api-key"

# Or use the flag
see --api-key "your-api-key" <command>
```

### Tùy chọn cấu hình

| Option | Environment Variable | Description |
|--------|---------------------|-------------|
| `--api-key` | `SEE_API_KEY` | API key S.EE của bạn (bắt buộc) |
| `--base-url` | `SEE_BASE_URL` | Base URL API tùy chỉnh |
| `--timeout` | `SEE_TIMEOUT` | Thời gian chờ yêu cầu |
| `--json` | - | Bật định dạng đầu ra JSON |

## Lệnh

### Domains & Tags

```bash
# List available domains for short URLs
see domains

# List available domains for file uploads
see file domains

# List your tags
see tags
```

### URL ngắn

```bash
# Create a short URL
see url create https://example.com

# Create with custom options
see url create https://example.com \
  --slug my-link \
  --domain s.ee \
  --title "My Link" \
  --password secret \
  --expires 2024-12-31

# Update a short URL
see url update --domain s.ee --slug my-link --title "New Title"

# Delete a short URL
see url delete --domain s.ee --slug my-link
```

### Đoạn văn bản

```bash
# Create text from stdin
echo "Hello World" | see text create --title "greeting"

# Create text from file
see text create --file ./snippet.txt --title "Code Snippet"

# Create with options
cat script.sh | see text create \
  --title "Shell Script" \
  --domain fs.to \
  --password secret \
  --expires 2024-12-31

# Update text
see text update --domain fs.to --slug abc123 --title "Updated Title"

# Delete text
see text delete --domain fs.to --slug abc123
```

### Tải tệp lên

```bash
# Upload a file
see file upload ./image.png

# Upload with custom domain
see file upload ./document.pdf --domain fs.to

# Upload from stdin
cat image.png | see file upload --stdin --filename image.png

# Delete a file
see file delete <delete-hash>
```

## Đầu ra JSON

Dùng flag `--json` để lấy đầu ra có thể đọc bởi máy, hữu ích cho scripting:

```bash
see url create https://example.com --json | jq '.short_url'
```

## Ví dụ

### Tạo URL ngắn và sao chép vào clipboard (macOS)

```bash
see url create https://example.com --json | jq -r '.short_url' | pbcopy
```

### Tải ảnh chụp màn hình lên và lấy URL (Linux)

```bash
see file upload ~/screenshot.png --json | jq -r '.url'
```

### Tạo URL ngắn hàng loạt

```bash
while read url; do
  see url create "$url" --json
done < urls.txt
```
